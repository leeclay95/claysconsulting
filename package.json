{
  "name": "claysconsulting",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "claysconsulting.org — static Astro site served from a Cloudflare Worker",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@cloudflare/vitest-pool-workers": "0.20.3",
    "astro": "^7.2.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.1"
  }
}
